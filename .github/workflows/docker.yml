name: Docker tests
on: [push]

jobs:
  my_first_job:
    runs-on: ubuntu-latest
    container:
      image: debian:stable-slim
      env:
        NODE_ENV: development
    steps:
    - name: My first step
      run: |
        pwd
        echo ${HOME}
        echo ${GITHUB_WORKSPACE}
        echo ${NODE_ENV}
